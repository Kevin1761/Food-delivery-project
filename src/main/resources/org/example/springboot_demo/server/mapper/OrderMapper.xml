<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace ="org.example.springboot_demo.server.mapper.OrderMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into orders (number, status, user_id, 
            <if test="addressBookId != null">address_book_id, </if>
            order_time, checkout_time, delivery_time, pay_method, pay_status,
            amount, remark, phone, address, 
            <if test="consignee != null">consignee, </if>
            estimated_delivery_time, delivery_status,
            pack_amount, tableware_number, tableware_status)
        values (#{number}, #{status}, #{userId}, 
            <if test="addressBookId != null">#{addressBookId}, </if>
            #{orderTime}, #{checkoutTime}, #{deliveryTime}, #{payMethod}, #{payStatus},
            #{amount}, #{remark}, #{phone}, #{address}, 
            <if test="consignee != null">#{consignee}, </if>
            #{estimatedDeliveryTime}, #{deliveryStatus},
            #{packAmount}, #{tablewareNumber}, #{tablewareStatus})
    </insert>

    <update id="updateStatus">
        update orders
        set status = #{status}
        where id = #{id}
    </update>
</mapper>